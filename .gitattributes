# 줄바꿈 정규화. 저장소에는 LF, 윈도우 작업 폴더에는 CRLF.
* text=auto

# 맥/리눅스에서 실행하는 스크립트는 반드시 LF. CRLF면 bash가 실패한다.
*.sh text eol=lf

# 윈도우 전용 스크립트는 CRLF 유지.
*.ps1 text eol=crlf
*.bat text eol=crlf

# 바이너리 — 변환 금지.
*.png binary
*.ico binary
*.icns binary
*.pdf binary
*.zip binary
*.exe binary
*.dll binary
