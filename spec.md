# ADF — 사내용 PDF 편집기 기능 명세서

> 소스 변경 추가: **도장·서명 이미지 보관함**, **PDF 두 버전 비교**를 추가했다. 이번 변경의 사용법과 범위는 README의 「소스에 추가된 기능 — 설치본 미생성」을 따른다. 이전 절의 보관함 미지원 문구는 기존 배포본에 대한 기록이며, 설치본·배포 파일·버전은 이번 작업에서 갱신하지 않는다.

- 버전: v0.1 (초안)
- 작성일: 2026-09-08
- 상태: 검토 대기 — 미결정 사항은 §8 참고

## 1. 개요

Acrobat 유료 기능 중 실제로 쓰는 기능만 담은 사내 전용 Windows PDF 도구.

- 프로그램 이름: **ADF**
- 파일 형식: **표준 .pdf 그대로 사용.** ADF는 프로그램 이름일 뿐 새 확장자를 만들지 않음 — 다른 회사/프로그램과 파일 호환이 깨지면 안 되므로 읽고 쓰는 파일은 전부 표준 PDF
- 배포: 사내 전용, 비상용. 인스톨러로 1회 설치
- 대상 OS: Windows 10 / 11 (64bit)
- 대원칙: **원본 비파괴** — 모든 편집 결과는 기본 "다른 이름으로 저장", 원본 덮어쓰기는 확인 후에만

## 2. 기술 스택 (제안)

| 역할 | 선택 | 이유 |
|---|---|---|
| 언어 | Python 3.12+ | 개발 속도, 라이브러리 생태계 |
| PDF 엔진 | PyMuPDF (fitz) | 렌더링·병합·분리·텍스트·이미지·압축을 라이브러리 하나로 전부 커버 |
| UI | PySide6 (Qt 6) | 썸네일 그리드, 드래그앤드롭, 미리보기 같은 데스크톱 UI에 최적 |
| 이미지 처리 | Pillow | 클립보드 이미지(도장) 처리 |
| 패키징 | PyInstaller + Inno Setup | exe 생성 + 설치 시 레지스트리 등록까지 |

라이선스: PyMuPDF는 AGPL이지만 **사내 내부 사용은 아무 의무가 발생하지 않음**(외부 배포나 외부 대상 웹서비스 제공 시에만 소스 공개 의무). PySide6(LGPL), Pillow·pypdf(허용적 라이선스)도 문제 없음.

대안 스택(참고): C# + WPF + PDFium — Windows 통합은 자연스럽지만 PDF 편집 기능 커버리지가 낮아 라이브러리를 여러 개 조합해야 하고, 텍스트 수정·압축 쪽 공수가 커짐. 비추천.

## 3. Windows 통합

### 3.1 설치 시 등록 (Inno Setup이 처리)
- `.pdf` 열기 프로그램 **후보** 등록: ProgId `ADF.Document` + RegisteredApplications
- 우클릭 메뉴 등록 (§3.3)
- 시작 메뉴 바로가기, 앱 아이콘

### 3.2 기본 프로그램 지정 (더블클릭으로 열리게)
- Windows 10/11 정책상 프로그램이 **스스로를 기본 앱으로 지정하는 것은 불가능**(모든 프로그램 공통 — 사용자 동의 필수)
- 대응: 최초 실행 시 "ADF를 기본 PDF 프로그램으로 지정할까요?" 안내 → Windows의 기본 앱 선택 대화상자를 바로 띄움 → 사용자가 목록에서 ADF 선택 (1클릭으로 끝)

### 3.3 우클릭(콘텍스트) 메뉴
- `.pdf` 1개 선택 시: **ADF로 열기 / PDF 분리…**
- `.pdf` 여러 개 선택 시: **ADF로 병합…** → 선택한 파일들이 로드된 병합창이 열림
- Windows 11 주의: 클래식 레지스트리 방식 메뉴는 "추가 옵션 표시"(Shift+F10) 아래에 표시됨. 최상위 노출이 필요하면:
  - (a) 사내 PC에 클래식 메뉴 복원 레지스트리 일괄 적용 ← 간단, 권장
  - (b) MSIX 패키징 + IExplorerCommand 구현 ← 공수 큼, 필요해지면 나중에

## 4. 화면 구성

메인 창 = 뷰어 + 썸네일 사이드바 + 툴바.

1. **뷰어 영역**: 현재 페이지 표시 (보기 모드는 §5.3)
2. **썸네일 사이드바**: 페이지 편집(이동·삭제·교체·빈 페이지)이 전부 여기서 일어남
3. **툴바**: 열기/저장, 보기 모드 전환, 확대/축소, 기능 버튼(병합·분리·넘버링·저용량 저장 등)
4. 별도 대화창: 병합창, 분리창, 넘버링창, 저용량 저장창

## 5. 기능 명세

### 5.1 병합
- 진입: 앱 내 버튼 / 탐색기에서 여러 PDF 선택 후 우클릭
- 파일 목록에 첫 페이지 썸네일 + 파일명 + 페이지 수 표시
- **드래그로 순서 변경** (+ 위/아래 버튼), 파일 추가/제거, 창으로 파일 드래그앤드롭
- 결과는 새 파일로 저장
- [확장 — Phase 2] 파일을 펼쳐서 페이지 단위로 섞어 병합

### 5.2 분리
- 진입: 우클릭 "PDF 분리…" / 앱 내
- 전체 페이지 **썸네일 미리보기** + 클릭·드래그로 범위 선택
- 분리 모드 4가지:
  1. 범위 1개 추출: n ~ m 페이지 → 파일 1개
  2. 다중 범위: `1-3, 7, 12-끝` 식 입력 또는 썸네일 사이에 구분점 찍기 → 범위별로 여러 파일
  3. 전부 낱장: 페이지당 1파일
  4. N페이지 단위로 자르기
- 옵션: 추출한 부분을 뺀 "나머지"도 같이 저장할지
- 출력 파일명 자동 생성: `원본명_p001-003.pdf` (수정 가능)

### 5.3 뷰어
- 보기 모드 4종: **한 페이지 / 연속 스크롤 / 두 쪽 펼침 / 여러 페이지(격자)**
- 펼침 방향: 기본 **왼쪽 먼저(좌철)**, 설정에서 오른쪽 먼저(우철) 전환
- 펼침 옵션: 표지(1페이지)를 단독 페이지로 둘지 여부
- **전체 화면** (F11)
- 확대/축소(Ctrl+휠), 폭 맞춤 / 페이지 맞춤 / 배율 직접 입력
- 페이지 이동: PgUp/PgDn, 페이지 번호 입력, 썸네일 클릭
- 성능: 화면 근처 페이지만 렌더링(지연 로딩), 썸네일 캐시

### 5.4 페이지 넘버링
- **위치**: 좌/중/우 × 상단/하단 = 6지점 버튼 + 여백(mm) 상하·좌우 입력
- **서식**:
  - 시작 번호, 적용 범위(전체 또는 n~m — 표지 제외용)
  - 자릿수 0채움: `1` / `01` / `001` 중 선택
  - 접두/접미 텍스트 자유 입력: 예) `3p`, `p.3`, `- 3 -`
- **폰트**: 시스템 폰트 목록에서 선택(한글 폰트 포함), 크기, 색
- **실시간 미리보기**: 대화창 안에 실제 페이지가 보이고, 설정을 바꿀 때마다 번호가 해당 위치에 즉시 표시
- 옵션 [Phase 3]: 홀/짝 페이지 좌우 반전(제본 인쇄용)
- 구현 방식: 페이지 위 텍스트 오버레이(원본 내용 불변), 사용한 폰트는 서브셋으로 임베드

### 5.5 페이지 편집 (썸네일 사이드바에서)
공통: 다중 선택(Ctrl/Shift), 다단계 실행 취소(Ctrl+Z), 저장 전까지 원본 불변

- **이동**: 썸네일 드래그 → 떨어질 위치의 페이지 사이가 **벌어지면서** 삽입 지점이 눈에 보임
- **삭제**: Del 키 / 우클릭 메뉴
- **빈 페이지 추가**: 선택 페이지 앞/뒤 선택, 크기는 "현재 페이지와 동일"이 기본(A4/A3/Letter도 선택 가능)
- **교체**: 페이지 선택 → "교체" → 다른 PDF 열기 → 그 PDF의 썸네일 그리드에서 가져올 페이지 선택(다중 가능, 미리보기로 확인) → 확인. 1페이지를 여러 페이지로 교체하는 것도 허용
- **회전** (좌/우 90°): 요청에는 없었지만 공수가 거의 0이라 포함 추천

### 5.6 텍스트 수정 ★ 유일하게 제약이 있는 기능

배경 — 왜 어려운가:
- PDF에는 문단·줄바꿈 개념이 없고 "이 좌표에 이 글자를 찍어라"만 저장됨 → 수정 후 자동 줄바꿈 재배치(리플로우)는 구조적으로 불가능에 가까움 (Acrobat도 이걸 완벽히 못 함)
- 폰트가 임베드돼 있긴 하지만 대부분 **문서에 실제 쓰인 글자만 담은 "서브셋"** → 원본에 없던 글자를 새로 입력하면 글리프가 없어서 대체 폰트가 필요해지는 경우가 많음

1차 지원 범위:
- 텍스트 **한 줄(스팬) 단위** 선택 → 내용 교체 또는 삭제
- 원본 스팬의 폰트명/크기/색을 읽어서, 같은 폰트가 시스템에 있으면 그대로 사용
- 같은 폰트가 없으면 **"이 폰트가 시스템에 없습니다. ○○(으)로 임시 대체할까요?"** 대화창 → 추천 대체 폰트 수락 또는 직접 선택
- 교체 텍스트가 원래 자리보다 길 때: 그대로 넘치게 둘지 / 글자 크기를 자동 축소할지 선택

미지원 (명시적으로 범위 밖):
- 문단 단위 리플로우
- 원본 임베드 서브셋 폰트에 새 글자 추가

### 5.7 이미지·객체
- **추가**:
  - 파일에서 삽입 (PNG/JPG — PNG 투명 배경 유지, 도장 용도)
  - **클립보드 붙여넣기(Ctrl+V)** → 마우스 위치에 배치
- **조작**: 드래그로 이동, 모서리 핸들로 크기 조절(비율 유지 기본), 삭제
- **기존 이미지 삭제**: 페이지 안의 이미지를 클릭해 선택 → Del
- [Phase 3] 도장 보관함: 자주 쓰는 이미지를 등록해두고 꺼내 쓰기

### 5.8 저용량 저장
- **방식 A — 전체 픽셀화**: 각 페이지를 이미지로 렌더링한 뒤 이미지 PDF로 재구성
  - 설정: DPI(72~300), JPEG 품질
  - 경고 표시: 저장 후 텍스트 검색·복사·수정이 불가능해짐
- **방식 B — 이미지만 최적화**: 텍스트·벡터는 그대로 두고 내장 이미지만 다운샘플링 + 재압축
  - 설정: 최대 DPI, JPEG 품질
- 용량 결정 방법 (두 방식 공통, 사용자가 택1):
  1. **직접 설정**: 품질/DPI 슬라이더
  2. **목표 용량**: "5MB 이하" 입력 → 품질을 자동으로 2~4회 탐색해서 목표에 맞춤
- 저장 전 요약 표시: 원본 ○○MB → 결과 ○○MB

## 6. 개발 단계

| 단계 | 내용 | 비고 |
|---|---|---|
| Phase 1 | 뷰어(보기 모드 전부) + 병합/분리 + 페이지 편집(이동·삭제·빈 페이지·교체·회전) + 인스톨러/우클릭/기본앱 후보 등록 | 이것만으로 일상 업무 대부분 커버 |
| Phase 2 | 넘버링 + 저용량 저장 + 이미지 추가/삭제 | |
| Phase 3 | 텍스트 수정(제한 범위) + 도장 보관함 + 단축키·설정·최근 파일 등 다듬기 | |

## 7. 비기능 요구사항
- 시작 속도: 더블클릭 → 첫 페이지 표시까지 2초 이내 (일반 문서 기준)
- 대용량: 500페이지 이상 문서에서도 썸네일 지연 생성으로 멈춤 없음
- 암호 PDF: 열람 암호 입력창 지원
- 실행 취소: 페이지 편집 계열은 다단계 undo
- UI 언어: 한국어

## 8. 미결정 사항 (검토 후 확정할 것)
1. 스택 확정 — Python + PySide6 + PyMuPDF 제안대로 갈지
2. Win11 우클릭 최상위 노출 — 클래식 메뉴 복원 레지스트리를 사내 PC에 적용할지, "추가 옵션" 하위로 만족할지
3. 편집 제한(소유자 암호)이 걸린 PDF — 사내 도구니까 제한을 무시하고 편집을 허용할지, 차단할지
4. 저장 시 원본 자동 백업(.bak)을 남길지
5. 앱 아이콘과 표시 이름 ("ADF" 그대로 갈지)

## 9. 구현 및 배포 정정 — 2026-09-08

원본 요구사항은 위에 보존한다. 이번 구현과 배포에서는 아래 결정이 앞의 제안보다 우선한다.

- **배포 대상**: Windows 10 1809 이상 / Windows 11, x64. Python 3.12 + PySide6 + PyMuPDF로 만들고 필요한 실행 환경을 설치 파일에 포함한다. 직원 PC에서 Python이나 개발 도구를 설치하지 않는다. 설치는 현재 사용자에게 관리자 권한 없이 수행하며 오프라인에서 실행한다.
- **실제 결과물**: `release/ADF-Setup-0.1.0.exe`, ADF 소스 ZIP, 사용하는 외부 라이브러리의 대응 원본 소스 ZIP, SHA-256 목록. `dist/ADF` 전체 폴더는 설치 없는 실행용으로도 쓸 수 있다.
- **기본 PDF 앱**: 설치 시 `ADF.Document`와 RegisteredApplications를 현재 사용자에 등록하여 연결 프로그램 후보가 된다. 기본 앱 변경은 Windows 설정 또는 연결 프로그램 대화상자에서 사용자가 직접 한다. Windows 버전마다 필요한 단계가 다르므로 “1클릭으로 끝”을 보장하지 않는다. 기존 기본 앱 값과 `UserChoice`를 강제로 수정하지 않는다.
- **탐색기 메뉴**: PDF 한 개에 대해 열기/분리 메뉴를 등록한다. Windows 11에서는 “추가 옵션 표시”에 나타난다. 클래식 메뉴를 복원하는 시스템 레지스트리 변경은 적용하지 않는다. 여러 파일 병합은 앱의 병합창이나 드래그앤드롭으로 시작한다. 탐색기에서 여러 파일을 한 번에 정확히 수집하는 shell 확장이 없는 상태에서는 다중 병합 메뉴를 등록하지 않는다.
- **문서 권한과 저장**: 소유자가 제한한 편집/추출 권한을 존중한다. 편집은 메모리에서 수행하고, 기본 저장은 다른 이름으로 저장이다. 기존 파일 덮어쓰기는 명시적으로 확인한 다음 처리한다. 개인 PDF를 설치·제거 대상으로 취급하지 않는다.
- **라이선스 정정**: §2의 “사내 내부 사용은 아무 의무가 발생하지 않음” 및 “문제 없음”이라는 단정은 철회한다. PyMuPDF/MuPDF는 AGPL 또는 Artifex 상용 라이선스이고, 이 빌드는 AGPL 배포판을 사용한다. Qt/PySide는 LGPL 및 구성 요소별 조건을 따른다. 조직 내부 사용·제3자 배포·수정·네트워크 제공을 구분해야 하며 원문 고지와 대응 소스 제공 조건을 확인한다. 새로 작성한 ADF 코드의 AGPL 전문, 외부 라이브러리 고지와 소스를 배포물에 동봉한다. 자세한 내용은 `LICENSES/README.md`에 기록한다.
- **macOS**: 공통 소스와 Mac에서 실행할 `.app`/`.dmg` 빌드 스크립트를 제공한다. Windows에서 Mac 바이너리를 생성하지 않으며 이번 환경에서 macOS 실기 검증은 수행하지 않았다.
- **성능 목표**: 2초 시작과 500페이지 사용성은 목표이며 모든 PC·PDF에서 달성했다는 보장으로 표시하지 않는다. 실제 빌드 검증 결과는 `release`의 smoke-test 결과와 테스트 기록에 남긴다.
- **이번 버전의 경계**: 문단 리플로우, OCR, 도장 보관함, 탐색기 최상위 shell 확장, 디지털 서명, PDF/A 검증은 포함하지 않는다. 압축의 목표 용량은 문서 내용에 따라 달성되지 않을 수 있으며 실제 결과 용량을 확인한다. 회사 인증서가 제공되지 않아 Windows 설치 파일은 미서명 상태다.
- **이미지 편집의 구현 범위**: 새 이미지의 드래그 이동·크기 조절은 “이미지 적용” 전에 지원한다. 이미 적용한 이미지는 실행 취소 후 재삽입하며, 기존 이미지 선택 모드는 삭제를 지원한다. 저장된 기존 이미지의 직접 이동·크기 변경은 후속 범위다. 텍스트 수정은 스팬 단위로 제한한다.

확인한 원문: [Windows 기본 앱 설정](https://learn.microsoft.com/en-us/windows/apps/develop/launch/launch-default-apps-settings), [Qt LGPL 의무](https://www.qt.io/development/open-source-lgpl-obligations), [GNU 내부 배포 FAQ](https://www.gnu.org/licenses/gpl-faq.html#InternalDistribution), [PyMuPDF](https://pymupdf.readthedocs.io/en/latest/about.html).

## 10. v0.2.0 탐색기 병합·분할과 독립 작업창

§9는 0.1.0 배포 기록이며 이번 추가 요구에는 아래 내용이 우선한다.

- PDF 한 개를 선택한 탐색기 우클릭 메뉴에는 **ADF로 PDF 분할…**, 두 개 이상의 PDF만 선택한 메뉴에는 **ADF로 PDF 병합…**을 표시한다. 혼합 파일 선택이나 파일 없는 선택에는 두 작업을 표시하지 않는다.
- 네이티브 `IShellExtInit` / `IContextMenu` 확장이 탐색기의 전체 선택 목록을 받아 한 요청으로 전달한다. 파일별 개별 실행을 일정 시간 동안 모으는 방식이나 긴 명령행에 의존하지 않는다.
- 병합/분할 메뉴를 누르면 해당 작업창만 독립적으로 열린다. 편집기 메인 창을 먼저 띄우지 않는다. 선택된 파일이 같은 순서로 들어오며 병합창에서 순서와 페이지를 조정할 수 있다. 일반 `ADF로 열기`는 전체 편집기를 연다.
- `ADFShell-0.2.0.dll`은 `ADF.exe` 옆에 설치한다. COM CLSID는 `{8093F936-820B-4CDB-A64B-7A39EC807A11}`, 스레딩 모델은 `Apartment`다. 현재 사용자의 `SystemFileAssociations\.pdf\shellex\ContextMenuHandlers\ADF`에 등록한다.
- 설치 AppId는 0.1.0과 동일하게 유지하여 사용자가 새 설치 파일을 실행하면 업데이트한다. 이전 `ADF.Split` 정적 메뉴는 정리하고 새 선택 메뉴를 등록한다. 기본 PDF 앱과 `UserChoice`는 유지한다. 탐색기를 강제로 종료하지 않는다.
- Windows 11 기본 간소화 메뉴에서는 “추가 옵션 표시” 안에 나타난다. 이번 대상은 Intel·AMD x64 Windows이며 ARM64 네이티브 탐색기 확장은 제공하지 않는다. Mac용 앱 빌드 스크립트에는 Windows 탐색기 확장을 넣지 않는다.
- 배포 파일은 `ADF-Setup-0.2.0.exe`, 버전별 소스 ZIP·외부 소스 ZIP·사용 안내·SHA-256 목록이다. 이전 0.1.0 배포 파일은 보존한다.
- 기존 사용자 설치를 건드리지 않는 검증 모드에서 같은 설치 파일을 임시 폴더와 전용 HKCU 하위 키에 설치하고 실행·제거한다. 기존 설치 파일의 해시, 바로가기, 등록 정보, 기본 PDF 연결을 비교한다. 네이티브 선택 메뉴는 실제 COM 객체와 Shell 선택 데이터로 검증한다.

원문: [Microsoft Shell 확장 등록](https://learn.microsoft.com/en-us/windows/win32/shell/handlers), [SystemFileAssociations의 ContextMenuHandlers 예시](https://learn.microsoft.com/en-us/windows/win32/wic/-wic-integrationregentries).

## 11. v0.3.0 편집 화면과 ADF 아이콘

§10까지는 이전 버전의 기록이며 0.3.0에는 아래 결정이 우선한다.

- 탐색기 우클릭 메뉴의 **ADF로 PDF 병합…**과 **ADF로 PDF 분할…** 앞에 같은 ADF 아이콘을 표시한다. 아이콘은 작은 메뉴 크기에서도 알아보기 쉬운 파란 문서와 흰색 A·연필 형태를 사용한다.
- 편집기의 파란 ADF 글자 표시는 없애고 파일·편집 기능을 하나의 상단 도구 막대에 배치한다. 창 폭이 좁으면 도구 막대가 두 줄로 자동 전환한다.
- 탐색기에서 시작하는 병합은 여러 파일을 새 PDF로 저장하는 독립 작업이다. 열린 문서 안의 **PDF 추가**는 파일 한 개 이상을 골라 현재 페이지 앞·뒤 또는 문서 끝에 삽입하며, 파일 순서와 각 파일의 페이지 범위를 정할 수 있다.
- 페이지 번호 화면은 밝은 배경과 설정 그룹을 사용하고, 실제 페이지 미리보기를 보면서 위치·여백·글꼴·크기·색·번호 범위를 조정한다.
- **텍스트 수정** 모드에 들어가 페이지의 텍스트 한 줄을 클릭하면 파란 선택 상자가 나타난다. 상자 이동과 크기 조절을 지원하고, 같은 상단 영역에 내용·글꼴·크기·색·적용 기능을 표시한다.
- 텍스트·이미지·페이지 번호와 페이지 작업은 모두 메모리의 작업 문서에 반영한다. 각각 적용할 때 원본 파일을 쓰지 않으며, 사용자가 마지막에 **다른 이름으로 저장**할 때 한 번에 PDF로 기록한다.
- 문단 리플로우와 저장된 기존 이미지의 직접 이동·크기 변경은 계속 지원 범위 밖이다. 새 이미지의 위치·크기는 적용 전에 조절한다.
- Windows 설치 파일은 `ADF-Setup-0.3.0.exe`이며 기존 AppId를 유지해 이전 설치를 업데이트한다. `ADFShell-0.3.0.dll`을 함께 설치하고, Windows 11에서는 메뉴가 **추가 옵션 표시** 안에 나타난다.

## 12. v0.3.1 빈 작업 화면과 문단 텍스트 수정

§11까지는 이전 버전의 기록이며 0.3.1에는 아래 결정을 우선한다.

- PDF 없이 실행하거나 문서를 닫으면 빈 회색 작업 영역과 **PDF 열기**·파일 끌어 놓기 안내만 표시한다. 예제 문서, 대시보드, 최근 문서 목록을 빈 화면에 대신 표시하지 않는다. 문서가 필요한 도구는 파일을 열기 전까지 비활성화한다.
- **텍스트 수정** 모드에서 글자를 클릭하면 글꼴 속성 조각 하나가 아니라, 배치상 같은 문단으로 이어지는 텍스트를 선택한다. 같은 줄의 여러 조각과 줄바꿈으로 이어진 문단을 하나의 선택 상자로 다룬다.
- 문단 구분은 PDF의 배치 정보를 이용한다. 떨어진 열, 표의 셀, 별도 텍스트 영역은 보수적으로 분리하며, 모든 PDF에서 의미상 문장 경계를 정확하게 판별한다고 보장하지 않는다.
- 상단의 텍스트 도구에는 여러 줄 내용 입력창과 글꼴·크기·색 설정을 표시한다. **Enter**는 줄바꿈이며 **변경 적용** 또는 **Ctrl+Enter**로 현재 작업에 반영한다. 선택 상자는 이동과 크기 조절을 지원한다.
- 수정은 선택한 텍스트 영역에 한정한다. 문서 전체의 글과 페이지를 자동으로 밀어내는 재배치나 OCR은 포함하지 않는다. 기존 이미지와 선·도형을 유지한다.
- 텍스트·이미지·페이지 번호와 페이지 편집은 메모리의 작업 문서에 반영하며 원본 파일은 저장 전까지 변경하지 않는다. 마지막 **다른 이름으로 저장**으로 변경 내용을 한 번에 PDF에 기록한다.
- Windows 설치 파일은 `ADF-Setup-0.3.1.exe`이며 기존 AppId와 공통 ADF 아이콘을 유지한다. 탐색기 확장은 `ADFShell-0.3.1.dll`로 배포한다. 이전 버전 배포 파일과 아이콘 원본은 보존한다.

## 13. v0.3.2 페이지 번호 설정 — 2026-09-09

- 위치는 실제 페이지 미리보기 가장자리의 상단·하단 × 왼쪽·가운데·오른쪽 아이콘 버튼으로 고른다. 버튼은 페이지 밖에 배치하여 실제 번호를 가리지 않는다.
- **좌우 여백 | 상하 여백**을 한 줄에 표시한다. 좌우 여백은 왼쪽 번호에는 왼쪽 가장자리, 오른쪽 번호에는 오른쪽 가장자리에서 적용한다.
- **맞쪽 번호 · 좌우 대칭**을 지원한다. 한쪽 페이지에서 선택한 번호 위치를 반대쪽 페이지에는 대칭으로 적용한다. 가운데 위치는 유지한다. 페이지 쪽은 번호 값이나 적용 범위 순번이 아닌 문서의 실제 페이지 위치로 판단한다.
- 맞쪽 미리보기는 본문 뷰어와 같은 페이지 배열 함수를 사용한다. 왼쪽 먼저·오른쪽 먼저와 표지 단독 설정을 그대로 따르며, 오른쪽 먼저에서는 2·3쪽이 3·2쪽 순서로 보인다. 본문이 두 쪽 보기이면 대칭을 끈 상태에서도 같은 펼침을 보여 준다.
- 글꼴 이름 콤보, 편집 가능한 글자 크기 콤보, 색 사각형과 HEX 입력란을 **글꼴** 한 줄에서 조절한다.
- **번호 적용 페이지 | 시작 번호 | 앞에 0 넣기**를 한 줄에 표시한다. 적용 페이지의 빈 값과 공백은 전체를 뜻한다. 앞자리 0은 시작 번호와 적용 페이지 수로 계산한 마지막 번호의 자릿수에 맞춘다. 예: 1~120은 001~120, 98~103은 098~103, 두 페이지에 999부터 넣으면 0999·1000이다.
- 앞뒤 표시는 **없음 / p.1 / 1p** 중 고른다. 자유 접두·접미 입력과 고정 1·01·001 선택은 이 화면에서 제거한다.
- 미리보기와 실제 PDF에 같은 번호 문자열·좌우 위치 계산을 사용한다. 잘못된 페이지 범위·색상 코드·글자 크기는 미리보기 아래에 설명하고 적용을 막는다.
- 번호 적용은 메모리의 작업 문서만 바꾸며 마지막 저장 때 다른 변경과 함께 기록한다. 설치 파일은 `ADF-Setup-0.3.2.exe`이고 이전 설치를 업데이트한다.

## 14. v0.3.3 화면 위 텍스트 편집과 페이지 추가 — 2026-09-09

§13까지의 기록과 다른 내용은 아래 결정을 우선한다.

- **텍스트 수정**만 눌렀을 때 추가 도구줄이나 설명 입력창을 표시하지 않는다. 문단을 클릭했을 때만 글꼴·크기·색과 완료 버튼을 표시하며, 내용은 PDF 페이지에 겹친 편집 영역에서 직접 입력한다. 줄바꿈을 포함한 문단 선택을 유지하고 테두리 이동·모서리 크기 조절을 지원한다.
- 빈 곳이나 다른 문단 클릭, 완료, Ctrl+Enter, 최종 저장 시 수정 내용을 현재 작업에 반영한다. 입력 중 선택한 글꼴과 크기를 표시하고 문단 상자를 페이지 안에서 필요한 높이까지 늘린다. 다른 문단과 페이지는 자동으로 밀어내지 않는다.
- 편집 중 원본 문단을 지운 페이지 미리보기는 한 번만 만든다. 타이핑할 때마다 PDF를 다시 교정하지 않는다. 작업 오류는 버전과 예외 경로를 로컬 오류 기록에 남긴다.
- 왼쪽 아래 **+**는 페이지 추가 메뉴이며 **빈 페이지 추가 / 클립보드를 페이지로 추가 / 파일로 추가**를 제공한다. 클립보드는 이미지와 복사한 파일을 지원한다. 파일은 PDF·PNG·JPG/JPEG를 지원한다.
- 파일 한 개는 선택 위치에 바로 넣는다. 여러 파일은 기존 병합 목록과 같은 순서 조정 창을 거쳐, 지정한 한 위치에 차례대로 페이지를 추가한다. 이미지 한 개는 한 페이지가 되며 EXIF 방향과 투명도를 유지한다. 여러 파일 추가도 실행 취소 한 번으로 되돌린다.
- 썸네일 사이에 마우스를 올리면 간격을 넓히고 **+**를 표시한다. 페이지나 파일을 드래그하는 동안에는 **+**를 숨기고 삽입 위치 선만 표시한다. 드롭 위치에 실제 페이지를 추가한다.
- 본문 오른쪽 위 보기 선택을 한 페이지·연속·양쪽면·격자 아이콘으로 표시한다. 왼쪽부터·오른쪽부터 보기 아이콘으로 펼침 방향을 바꾸며 페이지 번호 미리보기에도 같은 방향을 사용한다. 페이지 번호와 이동 버튼은 본문 미리보기 아래에 배치한다.
- 한 페이지 보기에서 휠로 페이지 끝을 넘으면 다음 페이지, 맨 위에서 올리면 이전 페이지를 표시한다. 확대 상태에서는 페이지 안을 먼저 스크롤한다.
- 썸네일 아래 버튼은 왼쪽 회전·오른쪽 회전·페이지 추가·교체·삭제의 다섯 개로 구성한다.
- 모든 변경은 최종 저장 전까지 원본 파일에 기록하지 않는다. 설치 파일은 `ADF-Setup-0.3.3.exe`이며 기존 AppId와 공통 ADF 아이콘을 유지한다.

검증 기록: Windows 소스 실행에서 가벼운 영문·한글 문서의 클릭·직접 입력·글꼴 변경·최종 저장을 확인했다. 보고된 `code=8: invalid key in dict`는 이 테스트에서 재현되지 않았다. 배포 설치 파일과 실행 파일은 생성했으나, 이 PC의 Windows Application Control 서명 정책이 새 실행 파일·설치 로더·네이티브 테스트 프로그램을 차단하여 배포 바이너리의 실행 및 설치 검증은 완료하지 못했다. 기존 설치와 기본 PDF 연결은 유지했다. 실패한 격리 테스트의 빈 임시 폴더·전용 레지스트리 키 정리도 도구의 승인 정책이 거부하여 남아 있다.

## 15. v0.3.4 PDF 내장 글꼴 재사용 — 2026-09-09

이전 절의 글꼴 대체 설명과 다른 내용은 아래 결정을 우선한다.

- 문단을 클릭하면 해당 PDF의 사용 가능한 내장 글꼴을 우선 사용한다. 내장 글꼴의 Unicode cmap이 없으면 PDF의 ToUnicode·CIDToGIDMap과 실제 렌더링 문자 정보를 이용해 복원한다. 원본 글리프 모양과 폭을 유지한다.
- 전체 글꼴과 일부 글자만 포함한 서브셋을 구분한다. 글리프 번호만 남고 실제 모양이 제거된 문자는 지원한다고 판단하지 않는다. 같은 문서의 다른 페이지에 남아 있는 문자도 Identity-H 문자 매핑으로 활용한다.
- 편집 화면은 글꼴 데이터를 앱 내부에만 임시 등록한다. PDF마다 고유한 미리보기 이름을 사용하여 PC의 동명 글꼴이나 다른 PDF의 서브셋과 충돌하지 않는다. 도구에는 원본 이름과 출처를 표시하며 선택 종료·문서 전환·앱 종료 시 임시 등록을 해제한다. PC에 글꼴을 설치하거나 다운로드하지 않는다.
- 재사용할 내장 글꼴이 없거나 새 글자를 지원하지 않으면 동일한 PC 글꼴을 확인한다. 그마저 사용할 수 없을 때만 **대체 글꼴로 편집 / 편집 취소**를 묻는다. 취소는 현재 문단 편집만 버리며, 앞서 반영한 다른 변경은 유지한다. 글꼴 메뉴에서 사용자가 직접 고른 글꼴은 명시적인 변경으로 처리한다.
- TrueType, OpenType 및 변환 가능한 비 CID CFF와 PDF 표준 글꼴을 지원한다. CID CFF·Type3 등 안전하게 재사용할 수 없는 형식이나 구분 불가능한 동명 리소스는 같은 PC 글꼴 확인 및 대체 선택 절차로 넘긴다. 기존 문단 편집의 가로쓰기·대표 글꼴 단일 스타일 범위는 유지한다.
- 미리보기의 글꼴 메트릭과 실제 문서 적용에 같은 글꼴 데이터를 사용한다. 글자 지원 여부를 확인한 다음 문단을 교체하며 실패 시 기존 문서를 복원한다. 원본 파일을 즉시 저장하지 않고 마지막 저장 시 글꼴을 포함해 기록한다.
- 검증 문서는 PC에 존재하지 않는 테스트 글꼴로 만든다. 내장 글꼴 복원, 다른 페이지의 문자, 없는 문자 검출, 미리보기 폭, 저장 후 글리프 이미지 일치, 취소·대체·원본 보존을 검사한다. 설치 파일은 `ADF-Setup-0.3.4.exe`다.

## 16. v0.3.5 미리보기 폭·화질과 글꼴 선택 기억 — 2026-09-09

- 왼쪽 패널 기본 폭은 260px이며 248~400px로 조절한다. 회전·추가·교체·삭제 버튼 5개를 유지하고 미리보기의 종이 폭과 행 높이를 패널에 맞춰 계산한다. 가로·세로·회전 페이지의 비율을 유지한다. 호버 삽입 간격은 현재 미리보기 높이에 40px를 더하며 드래그 중에는 숨긴다.
- 페이지 번호와 이동 버튼은 본문 영역 하단에 둔다. 왼쪽 패널 크기를 바꿔도 본문과 정렬을 유지한다.
- 본문과 미리보기는 Windows 장치 배율 및 확대율을 반영하여 화면 해상도보다 1.5배의 픽셀 밀도로 렌더링한다. 기존 3배 확대·500만 픽셀 상한으로 크게 확대할 때 저해상도 이미지를 늘리던 동작을 교체한다. 고배율·대형 페이지는 보이는 부분과 주변만 렌더링하며, 잘라낸 이미지의 실제 페이지 좌표를 유지한다. 한 이미지 1600만 픽셀, 본문 캐시 96MiB·18개, 썸네일 캐시 24MiB·48개로 제한한다.
- 같은 이름의 원본 글꼴과 편집 후 내장 글꼴이 함께 존재하면 문자별 실제 모양과 폭의 동등성을 확인하여 재사용한다. 이름만 같고 모양이 다른 글꼴은 자동으로 같다고 판단하지 않는다. 편집 후 및 저장·다시 열기 후의 재편집을 검사한다.
- 대체 선택 창에 **이 문서의 같은 글꼴에 이 선택 적용** 체크박스를 추가한다. 기본은 해제이며 사용자가 체크하고 대체를 허용했을 때만 같은 원본 글꼴의 대체 선택을 기억한다. 다른 글꼴은 별도로 확인한다. 문서를 닫거나 새로 열면 기억을 지우고, 기억한 글꼴이 새 문자를 지원하지 않으면 다시 확인한다. 취소는 선택을 기억하지 않는다.
- 최종 저장 전 원본 파일은 변경하지 않는다. 설치 파일은 `ADF-Setup-0.3.5.exe`다.

## 17. v0.3.6 파란 A 아이콘과 탐색기 투명도 — 2026-09-09

- 앱·탐색기·설치 아이콘은 배경 없이 파란 A만 사용한다. 문서 테두리, 접힌 모서리와 연필 장식은 제거한다.
- 제품용 원본은 `assets/adf-mark.svg`다. PNG·ICO·ICNS는 같은 벡터에서 내보내며, ICO는 작은 메뉴 크기 및 화면 배율별 프레임을 포함한다.
- 탐색기 병합·분할 메뉴는 32비트 투명 DIB에 아이콘을 그려 알파를 보존한다. 화면 DPI에 맞춰 메뉴 아이콘 크기를 정한다.
- 직원 설치·실행에는 `ADF-Setup-0.3.6.exe` 하나만 필요하다. 소스 ZIP은 배포 담당자가 별도로 제공하는 자료다.
- ICO 자체의 Windows 디코딩·투명도 검사와 컴파일된 탐색기 확장의 실행 검증을 구분한다. 현재 PC의 기존 실행 제한이 해소되지 않아 배포 바이너리의 설치·탐색기 통합 실행 검증은 보류한다.

## 18. v0.3.7 두 쪽 시작 위치·연속 보기·설치 글꼴 검색 — 2026-09-09

- 왼쪽 시작은 `[1 | 2], [3 | 4], [5 | 6]`, 오른쪽 시작은 `[빈칸 | 1], [2 | 3], [4 | 5], [6 | 빈칸]`이다. 첫 장을 가운데로 옮기거나 각 쌍의 순서를 뒤집지 않는다.
- 첫 페이지는 기본 왼쪽에서 시작한다. 이전 우철·표지 단독 설정은 사용하지 않고 새 시작 위치 설정을 저장한다. 빈칸은 화면상의 자리이며 실제 PDF에 페이지를 추가하지 않는다.
- 한 쪽씩, 한 쪽 연속, 두 쪽씩, 두 쪽 연속, 격자 보기를 아이콘으로 각각 선택한다. 두 쪽씩 보기에서는 휠·이전/다음·PgUp/PgDown으로 묶음을 이동한다. 확대 시 현재 묶음의 스크롤 끝에서 이동한다.
- 페이지 번호 미리보기의 첫 장·끝 장 빈칸과 좌우 대칭 여백도 동일한 시작 위치를 따른다. 빈칸에는 위치 버튼이나 번호가 없다.
- 설치 글꼴의 내부 한글·영문·전체·PostScript 이름을 조회한다. Windows 시스템·사용자 폴더와 등록된 경로를 확인하고, TTC·OTC에서는 선택한 글꼴을 추출해 편집·저장에 일관되게 사용한다. 같은 가족의 보통체와 굵은체를 구분한다.
- 실행 중 글꼴 설치를 반영하기 위해 목록을 갱신하며, 찾지 못했다는 결과를 계속 고정해 두지 않는다. 내장 글꼴 우선 재사용과 대체 여부 확인은 유지한다.
- 설치 파일은 `ADF-Setup-0.3.7.exe`다. 설치·탐색기 바이너리의 실제 실행은 이 PC의 기존 보안 제한으로 미검증이다.

## 19. v0.3.8 보기 그룹·저용량 저장 활성 상태 — 2026-09-09

- 보기 아이콘을 `한 쪽씩·연속 | 두 쪽씩·연속 | 전체 보기 | 왼쪽·오른쪽 시작` 네 그룹으로 나누고 그룹 사이에 1px의 옅은 회색 세로선을 넣는다.
- 시작 위치는 두 쪽씩·두 쪽 연속 보기에서만 조절할 수 있다. 한 쪽씩·한 쪽 연속·전체 보기에서는 비활성화한다. 두 쪽 보기로 돌아가면 기존 시작 위치를 유지한다.
- 저용량 저장에서 자동 품질 조절을 끄면 목표 용량 입력칸과 라벨을 비활성화한다. 켜면 최대 해상도·JPEG 품질 입력칸과 라벨을 비활성화하고 목표 용량을 활성화한다. 비활성 입력칸은 옅은 회색 배경과 글자로 구별한다.
- 자동 조절 체크박스는 해제 시 흰색, 선택 시 파란색과 흰 체크 표시로 구분한다. 전환해도 입력했던 값은 유지한다.
- 설치 파일은 `ADF-Setup-0.3.8.exe`다.

## 20. v0.3.9 시작 위치 아이콘 비활성 표시 — 2026-09-09

- 두 쪽 보기가 아닐 때 왼쪽·오른쪽 시작 아이콘의 그림 자체를 옅은 회색 `#C4CBD5`로 표시한다.
- 선택 여부와 관계없이 같은 비활성 그림을 쓰며 클릭을 차단한다. 두 쪽 보기로 돌아가면 원래 아이콘 진하기와 기존 시작 위치를 복원한다.
- 설치 파일은 `ADF-Setup-0.3.9.exe`다.
